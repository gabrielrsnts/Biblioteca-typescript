export default class Database {
    async executar(query: string, params: any[] = []): Promise<any[]> {
        // Implementação do banco de dados aqui
        // Por enquanto, vamos retornar um array vazio
        return [];
    }
} 